VITE_API_URL=http://localhost:8080/api/v1
# Leave VITE_WS_URL unset in dev — the Vite proxy forwards /ws to the backend.
# Set explicitly in production, e.g. wss://your-domain.com
# VITE_WS_URL=ws://localhost:8080
